import styled from 'styled-components';

const Box = styled.div`
  background: #fff;
  margin-bottom: 15px;
  box-shadow: 0 1px 2px 0 rgba(0, 0, 0, 0.08);
  border-radius: 10px;
  font-size: 16px;
  line-height: 1;
`;

export default Box;
